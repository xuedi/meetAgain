{% if image %}
    <img src="{{ asset( image.assetPath ) }}" {% if image.alt %}alt="{{ image.alt }}"{% endif %} />
{% else %}
    <img src="/images/thumbnails/1_{{ size }}.png" alt="Placeholder IMAGE"/>
{% endif %}
