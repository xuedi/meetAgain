{% if image %}
    {% set file = image.id ~ '_' ~ size ~ '.' ~ image.extension %}
    <img src="/images/thumbnails/{{ file }}" {% if image.alt %}alt="{{ image.alt }}"{% endif %} />
{% else %}
    <img src="/images/thumbnails/1_{{ size }}.png" alt="Placeholder IMAGE"/>
{% endif %}
