{% extends 'admin/base.html.twig' %}

{% block title %}Admin - Config{% endblock %}

{% block content %}

    <div class="container">

        <table class="table">
            <thead>
            <tr>
                <th>name</th>
                <th>value</th>
                <th>type</th>
            </tr>
            </thead>
            <tbody>
            {% for item in config %}
                <tr>
                    <th>{{ item.name }}</th>
                    <th>{{ item.value }}</th>
                    <th>{{ item.type.value }}</th>
                </tr>
            {% endfor %}
            </tbody>
        </table>

    </div>

{% endblock %}

