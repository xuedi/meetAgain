{% extends 'admin/base.html.twig' %}

{% block title %}Admin - Events{% endblock %}

{% block content %}

    <div class="container">

        <table class="table">
            <thead>
            <tr>
                <th>ID</th>
                <th>eMail</th>
                <th>Roles</th>
            </tr>
            </thead>
            <tbody>
            {% for user in users %}
                <tr>
                    <th>{{ user.id }}</th>
                    <th>{{ user.email }}</th>
                    <th>{{ user.roles|join(' | ') }}</th>
                </tr>
            {% endfor %}
            </tbody>
        </table>

    </div>

{% endblock %}

