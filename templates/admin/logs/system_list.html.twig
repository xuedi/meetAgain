{% extends 'admin/base.html.twig' %}

{% block content %}

    <section class="section">
        <div class="container">
            <div class="columns">
                <div class="column is-3">
                    <ul>
                        {% for id,log in logs %}
                            <li><a href="{{ path('app_admin_logs_system', {'id': id}) }}">{{ log.name }}</a></li>
                        {% endfor %}
                    </ul>
                </div>
                <div class="column is-9">
                    <table class="table is-fullwidth">
                        <thead>
                        <tr>
                            <th>Date</th>
                            <th>Type</th>
                            <th>Level</th>
                            <th>Message</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for line in content %}
                            <tr>
                                <td><nobr>{{ line.date|date('Y-m-d H:i:s') }}</nobr></td>
                                <td>{{ line.type }}</td>
                                <td>{{ line.level }}</td>
                                <td>{{ line.message }}{# Toggle show {{ line.json }} #}</td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>


                </div>
            </div>
        </div>
    </section>

{% endblock %}
