<h1 class="title is-4">{{ 'location.page_title'|trans }}</h1>
<p class="subtitle is-6">{{ 'location.index_intro'|trans }}</p>

<div class="box">
    <h2 class="title is-5">{{ 'location.index_fix_heading'|trans }}</h2>
    <p>{{ 'location.index_fix_body'|trans }}</p>
</div>

<div class="box">
    <h2 class="title is-5">{{ 'location.index_new_heading'|trans }}</h2>
    <p class="mb-4">{{ 'location.index_new_body'|trans }}</p>
    <a class="button is-primary is-small" href="{{ path('app_location_suggest') }}">
        <span class="icon"><i class="fas fa-plus"></i></span>
        <span>{{ 'location.nav_suggest_new'|trans }}</span>
    </a>
</div>

<p class="has-text-grey is-size-7">{{ 'location.index_review_note'|trans }}</p>
